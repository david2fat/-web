# 圖片和影片設置指南

## 📸 支援的媒體類型

現在系統同時支援**圖片**和**影片**兩種格式！

## 📁 文件結構

```
public/
├── images/          # 圖片文件夾
│   ├── sunny.png
│   ├── sunny2.png
│   ├── rainy.png
│   ├── rainy2.png
│   ├── cool.png
│   └── cool2.png
└── videos/          # 影片文件夾（需要創建）
    ├── sunny.mp4
    ├── sunny2.mp4
    ├── rainy.mp4
    ├── rainy2.mp4
    ├── cool.mp4
    └── cool2.mp4
```

## ⚙️ 如何配置

### 方法 1: 使用圖片（預設）

圖片已經配置好，無需修改。系統會自動使用 `public/images/` 中的圖片。

### 方法 2: 使用影片

1. **創建影片文件夾**：
   ```bash
   mkdir public/videos
   ```

2. **放置影片文件**：
   - 將影片文件放到 `public/videos/` 資料夾
   - 建議格式：`.mp4`、`.webm`、`.mov`
   - 建議命名：
     - 男性：`sunny.mp4`、`rainy.mp4`、`cool.mp4`
     - 女性：`sunny2.mp4`、`rainy2.mp4`、`cool2.mp4`

3. **修改配置**：
   打開 `src/utils/avatarGenerator.js`，找到 `LOCAL_MEDIA` 配置：

   ```javascript
   MALE: {
     SUNNY: {
       type: 'video',  // 改為 'video'
       url: `${PUBLIC_URL}/videos/sunny.mp4`,  // 改為影片路徑
     },
     // ... 其他配置
   }
   ```

### 方法 3: 混合使用（部分圖片、部分影片）

您可以在同一個配置中混合使用圖片和影片：

```javascript
MALE: {
  SUNNY: {
    type: 'video',  // 晴天用影片
    url: `${PUBLIC_URL}/videos/sunny.mp4`,
  },
  RAINY: {
    type: 'image',  // 雨天用圖片
    url: `${PUBLIC_URL}/images/rainy.png`,
  },
  COOL: {
    type: 'video',  // 涼爽天氣用影片
    url: `${PUBLIC_URL}/videos/cool.mp4`,
  },
}
```

## 🎬 影片建議規格

為了最佳顯示效果，建議：

- **格式**：MP4 (H.264 編碼)
- **解析度**：建議 500x700 或更高
- **檔案大小**：盡量壓縮，建議 < 5MB
- **時長**：如果是循環播放，建議 3-10 秒
- **背景**：建議透明背景或與應用主題色調一致

## 🔄 切換媒體類型

修改 `src/utils/avatarGenerator.js` 中的 `LOCAL_MEDIA` 配置：

```javascript
const LOCAL_MEDIA = {
  MALE: {
    SUNNY: {
      type: 'image',  // 或 'video'
      url: `${PUBLIC_URL}/images/sunny.png`,  // 或 videos/sunny.mp4
    },
    // ...
  },
  // ...
};
```

## ✅ 功能特點

- ✅ 自動檢測媒體類型（圖片或影片）
- ✅ 影片自動循環播放
- ✅ 影片自動靜音（符合瀏覽器自動播放政策）
- ✅ 預載入機制，提升載入速度
- ✅ 錯誤處理，載入失敗時顯示備用圖標
- ✅ 向後兼容，現有圖片配置無需修改

## 🐛 常見問題

### Q: 影片無法自動播放？
A: 確保影片設置了 `muted` 屬性（已自動設置）。某些瀏覽器要求影片必須靜音才能自動播放。

### Q: 影片載入很慢？
A: 建議：
- 壓縮影片文件大小
- 使用較短的影片（3-10秒循環）
- 使用適當的解析度（不需要太高）

### Q: 如何測試影片？
A: 
1. 將影片放到 `public/videos/` 資料夾
2. 修改配置中的 `type` 為 `'video'` 和對應的 `url`
3. 重新啟動開發服務器：`npm start`

## 📝 注意事項

- 影片文件會增加應用大小，請注意部署時的檔案大小限制
- 建議使用壓縮過的影片格式
- 確保影片路徑正確，否則會顯示錯誤提示

